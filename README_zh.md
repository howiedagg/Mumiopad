# Mumiopad - 手機無線觸控板與虛擬鍵盤

Mumiopad 是一個可以將 Android 手機輕鬆變成電腦無線觸控板與鍵盤的工具。適合您在沙發上放鬆、觀賞影音、簡報，或是需要輕量遠端控制電腦的日常場景。

透過高效率的本機端連線，提供極流暢的游標追蹤與低延遲的輸入模擬，且完全不需要繁瑣的網路 IP 設定。

---

## 📥 快速開始（如何下載與連線）

請先點擊前往 👉 **[Mumiopad 最新版本下載頁面](https://github.com/howiedagg/Mumiopad/releases)** 取得以下檔案：

### 1. 電腦端設定（Windows）
*   在下載頁面中，點擊並下載 `MumiopadServer-vx.x.x.exe`。
*   **直接雙擊執行**下載好的 `.exe` 檔案。
*   執行後，程式會直接常駐於畫面右下角系統匣，並在背景默默為您服務，不佔用任何桌面空間。

### 2. 手機端設定（Android）
*   在下載頁面中，點擊並下載 `Mumiopad-vx.x.x.apk` 到您的手機並完成安裝。
*   確保手機與電腦連線至 **同一個 Wi-Fi 網路**。
*   打開手機 App，它會自動搜尋附近的電腦。點擊您的電腦名稱，電腦螢幕便會彈出配對視窗，點擊「是」即可完成連線！

---

## 🎮 觸控板手勢與功能介紹

我們設計了符合日常直覺的操作方式，並加入觸覺震動來輔助無視覺盲操：

*   **單指操作**：
    *   `移動游標`：在觸控板區域滑動。
    *   `滑鼠左鍵`：單指輕點一下。
    *   `長按並拖曳`：手指按住觸控板不動約 200 毫秒，手機會發出一次點擊震動，此時即可開始拖曳視窗或選取文字。手指抬起時會伴隨釋放震動。
*   **雙指操作**：
    *   `滑鼠右鍵`：雙指同時輕點。
    *   `滾動網頁`：雙指同時上下滑動（可在設定中調整滾動方向與速度）。
    *   `瀏覽器上一頁/下一頁`：在觸控板任何地方，雙指左右刷動。
*   **三指操作**：
    *   `多工作業檢視 (Win+Tab)`：三指輕點一下。
    *   `顯示桌面`：三指同時向下垂直滑動。
    *   `還原所有視窗`：三指同時向上垂直滑動。
*   **四指操作**：
    *   `開關虛擬鍵盤`：四指同時在觸控板輕點，即可快速叫出或收合手機鍵盤，直接在手機上輸入中英文。
*   **手機實體音量鍵**：
    *   在連線成功時，按手機側邊的實體音量鍵，即可直接調整電腦系統的主音量（未連線時自動恢復為調整手機音量）。

---

## ⚙️ 系統設定調整
您可以隨時點擊手機 App 右上角的齒輪圖示，進行以下微調：
*   **滑鼠移動速度**（支援 0.3x ~ 3.0x 靈敏度調整）。
*   **滾動速度**。
*   **反向滾動（自然滾動）** 開關。

---

<!-- 這裡將技術細節隱藏，一般使用者不會被干擾，但開發者可以點開閱讀 -->
<details>
<summary>🛠️ 開發人員資訊與技術細節 (點擊展開)</summary>

### 技術棧
*   **Android 端**：Kotlin, Jetpack Compose, OkHttp (WebSocket), mDNS (NsdManager).
*   **PC 伺服器端**：Python, websockets, pynput (輸入模擬), pystray (系統列選單).

### 專案架構
為了便於後續擴充與維護，專案採用低耦合設計：
*   `GestureEngine.kt` 為純 Kotlin 寫成的手勢狀態機，不依賴 Android UI 類別，透過 callback 將手勢指令與本地回饋傳遞出去，極易進行單元測試與手感微調。
*   `ScrollCatchupSmoother.kt` 將跨越死區時的位移分批補償，消除雙指滾動初期的遲滯感。
*   `WifiPerformanceManager.kt` 在連線期間持有 WiFi 低延遲鎖（Low Latency Lock），防止手機晶片因進入省電模式而產生週期性微卡頓。

### 手動建置伺服器環境指令 (開發偵錯用)
如果您想直接以源碼運行伺服器：
```bash
cd pc-server-python
python -m venv .venv
source .venv/bin/activate  # Windows 請執行 .venv\Scripts\activate
pip install -r requirements.txt
python server.py